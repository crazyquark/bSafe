services:
  server:
    build: ./server
    network_mode: host
    cap_add:
      - NET_ADMIN
    volumes:
      - ./server/data:/app/data
    environment:
      - SECRET_KEY=bsafe-dev-key-change-me
    restart: unless-stopped
